[project]
name = "backend"
version = "0.1.0"
description = "birdie - backend"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "fastapi>=0.141.1",
  "fastapi-cli>=0.0.32",
  "uvicorn>=0.52.4",
]

[tool.fastapi]
entrypoint = "main:app"
